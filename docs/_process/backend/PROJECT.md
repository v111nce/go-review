# Backend Process Project

## 背景

Go 规约治理需要把产品层的规则目录、违规检测和自动修复能力落到技术实现。当前文档阶段只定义技术契约，不进入代码实现。

## 当前后端主题

- Adapter 运行时和统一结果模型。
- Pipeline DAG 调度。
- 通用工具链 adapter 集成。
- 架构依赖约束 adapter。
- 自定义 `go/analysis` 语义 adapter。
- 自动修复安全策略。
- 报告输出 adapter。
